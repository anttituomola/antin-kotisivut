# AWS SES credentials for the contact form (src/lib/email.ts)
AWS_REGION=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
ADMIN_EMAIL=

# Mailing-list backend URL used by the /mail-api proxy route
# (src/pages/mail-api/[...path].ts). Defaults to the sslip.io hostname if unset.
MAIL_API_URL=
